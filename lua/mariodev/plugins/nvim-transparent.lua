require('nvim-transparent').setup({
  enable = true,
  -- extra_groups = { "NvimTree", "packer" },
  -- disable = { "dashboard" },
  -- default to 100
  max_percentage = 100,
  -- default to true
  on_off_commands = true,
  -- default to true
  set_root_buffer = true,
})
